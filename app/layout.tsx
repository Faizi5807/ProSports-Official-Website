import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] })
const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'ProSports Global | Premium Football Manufacturing',
  description: 'Global leader in professional and commercial football manufacturing. Bulk orders, custom designs, and competitive pricing for retailers and distributors.',
  keywords: ['football manufacturing', 'bulk orders', 'sports equipment', 'commercial production', 'wholesale'],
  openGraph: {
    title: 'ProSports Global | Premium Football Manufacturing',
    description: 'Quality footballs for professional, commercial, and training use.',
    type: 'website',
  },
}

export const viewport: Viewport = {
  colorScheme: 'light dark',
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#f8f7f6' },
    { media: '(prefers-color-scheme: dark)', color: '#1a1a1a' },
  ],
  userScalable: true,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
